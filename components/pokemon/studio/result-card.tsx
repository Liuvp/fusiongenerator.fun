"use client";

import { forwardRef } from "react";
import Image from "next/image";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Download, RefreshCw, Share2 } from "lucide-react";
import { Pokemon, FusionStyle } from "@/lib/pokemon-data";

interface ResultCardProps {
    result: {
        imageUrl: string;
        pokemon1: Pokemon;
        pokemon2: Pokemon;
        style: FusionStyle;
    };
    onDownload: () => void;
    onShare: () => void;
    onReset: () => void;
    onSwapLeft: () => void;
    onSwapRight: () => void;
    onTryAnotherPair: () => void;
}

export const ResultCard = forwardRef<HTMLDivElement, ResultCardProps>(({ result, onDownload, onShare, onReset, onSwapLeft, onSwapRight, onTryAnotherPair }, ref) => {
    return (
        <Card ref={ref} className="border-0 shadow-xl overflow-hidden mb-6 animate-in fade-in slide-in-from-bottom-8 duration-500 bg-white">
            <CardContent className="p-0">
                <div className="relative aspect-square w-full bg-gradient-to-br from-gray-50 to-gray-100/50">
                    <Image
                        src={result.imageUrl}
                        alt={`${result.pokemon1.name} and ${result.pokemon2.name} Fusion`}
                        fill
                        className="object-contain p-6 hover:scale-105 transition-transform duration-700"
                        unoptimized
                        sizes="(max-width: 768px) 100vw, 800px"
                    />

                    {/* Badge Overlay */}
                    <div className="absolute top-4 left-4 bg-white/90 backdrop-blur-sm px-3 py-1 rounded-full text-xs font-bold text-gray-600 shadow-sm border border-gray-100">
                        Generated by AI
                    </div>
                </div>

                <div className="p-5 space-y-5 bg-white">
                    <div className="text-center sm:text-left">
                        <h2 className="text-xl font-black text-gray-900 tracking-tight">
                            {result.pokemon1.name} <span className="text-gray-400 font-light">×</span> {result.pokemon2.name}
                        </h2>
                        <div className="flex items-center justify-center sm:justify-start gap-2 mt-1">
                            <span className="inline-block w-2 h-2 rounded-full bg-green-500"></span>
                            <p className="text-sm text-gray-500 font-medium">{result.style.name} style ready. Download or continue generating.</p>
                        </div>
                    </div>

                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                        <Button onClick={onDownload} className="bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white shadow-md hover:shadow-lg transition-all">
                            <Download className="w-4 h-4 mr-2" />
                            <span>Download HD</span>
                        </Button>
                        <Button onClick={onReset} variant="outline" className="border-gray-200 hover:bg-gray-100 transition-colors">
                            <RefreshCw className="w-4 h-4 mr-2" />
                            <span>Continue Generating</span>
                        </Button>
                        <Button onClick={onSwapLeft} variant="outline" className="border-gray-200 hover:bg-gray-100 transition-colors">
                            <span>Swap Left Pokemon</span>
                        </Button>
                        <Button onClick={onSwapRight} variant="outline" className="border-gray-200 hover:bg-gray-100 transition-colors">
                            <span>Swap Right Pokemon</span>
                        </Button>
                        <Button onClick={onShare} variant="outline" className="border-gray-200 hover:bg-blue-50 hover:text-blue-600 transition-colors sm:col-span-2">
                            <Share2 className="w-4 h-4 mr-2" />
                            <span>Share Result</span>
                        </Button>
                    </div>
                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                        <Button onClick={onTryAnotherPair} variant="outline" className="border-gray-200 hover:bg-gray-100 transition-colors">
                            <span>Try Another Popular Pair</span>
                        </Button>
                        <Link
                            href="/gallery?source=pokemon_result"
                            className="inline-flex items-center justify-center rounded-xl border border-gray-200 bg-white px-4 py-2.5 text-sm font-semibold text-gray-700 hover:bg-gray-50"
                        >
                            View Example Results
                        </Link>
                        <Link
                            href="/dragon-ball?source=pokemon_result"
                            className="inline-flex items-center justify-center rounded-xl border border-gray-200 bg-white px-4 py-2.5 text-sm font-semibold text-gray-700 hover:bg-gray-50 sm:col-span-2"
                        >
                            Try Dragon Ball Studio
                        </Link>
                        <Link
                            href="/pricing?source=pokemon_result_next_step"
                            className="inline-flex items-center justify-center rounded-xl bg-gradient-to-r from-purple-600 to-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:from-purple-700 hover:to-indigo-700 sm:col-span-2"
                        >
                            Unlock Unlimited
                        </Link>
                    </div>
                </div>
            </CardContent>
        </Card>
    );
});

ResultCard.displayName = "ResultCard";
