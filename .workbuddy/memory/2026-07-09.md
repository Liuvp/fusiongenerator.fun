# 2026-07-09 · Dragon Ball 转化漏斗全链路复检

逐文件读源码复检（不依赖记忆），报告：`.workbuddy/memory/funnel-audit-2026-07-09.html`

## 发现
- **P0（阻断·回归）Pro 付费墙全链路失效**：免费/匿名用户可真实生成 6 个 `pro:true` 角色（broly/jiren/whis/vegetto/gogeta/gotenks）。
  - 后端 `app/api/generate-fusion/route.ts:242-251` 无 `char.pro`/`isVIP` 校验（最致命，无二次兜底）。
  - 前端 `fusion-studio.tsx:1074` 写死 `isProLocked={false}`；`:607` `getRandomCharacters(2,false)`（应为 `!quota.isVIP`）；`:619/:633` swap 不过滤 pro；`:556` selectCharacter 无 pro 拦截。
  - `CharacterButton` 组件本身支持 `isProLocked`（🔒+琥珀样式 139-179），只需传正确值。
- **P1 品牌/SEO 泄漏**：`app/layout.tsx:29,31,33,35,42,44` 与 `app/page.tsx:14,16,21,23,38,40` 仍写 "Dragon Ball & Pokemon"，与软隐藏聚焦策略冲突。
- **P2 文案矛盾**：`app/pricing/page.tsx:10,18,39` meta 仍 "unlimited"；`components/dashboard/generation-quota-card.tsx:110` "Unlimited generations per day"（应为 300/月）；contact/about 页面 "unlimited"。

## 已验证健康（勿动）
- Creem webhook 3 层幂等（checkout_id + 5min 冷却 + Redis 10min）；PII 日志已清；匿名 2 次+突发保护+失败回退；免费 2 赠分 CAS+退款；Pro 300/月自然月滚动；定价可见内容已统一（Pro/$9.99/300/Save33%/Get Pro）；auth 闭环（dbReturnTarget=/dragon-ball#fusion-studio）；配额弹窗统一；导航软隐藏；sitemap 无 pokemon 字符串。

## 状态
- 本轮仅审计，未改代码（用户要求"彻底检查"）。P0 修复量小（后端 ~4 行 + 前端 4 处传参），建议立即执行，待用户确认。

## 用户两项决策（2026-07-09 14:00 追加，推翻原 P0/P1 标记）
1. **Pro 角色锁全开是刻意设计**：免费/付费用户都能选任意两个角色。变现点=生成次数（免费2/Pro300月），非角色锁。原"P0 付费墙泄漏"不再视为 bug，MEMORY.md 易回归风险段已改写为"刻意开放"。
2. **metadata "Pokemon" 刻意保留**（怕动影响 SEO 排名），不修改、不当问题提。

## 复检补验（除两项决策外，其余项再核）
- ✅ 原 P0#7 匿名失败扣配额、#13 注册免费失败扣配额：**均已修复**（generate-fusion catch 块登录 CAS 退款 + 匿名 Redis 回退）。
- ✅ 第三方 #8 "No thanks" 不关横幅：**已修复**（fusion-studio:1535 setResultBannerDismissed(true)）。
- ✅ 第三方 #10 dashboard credits_history 硬编码空：**已修复**（credits-balance-card 读 recentHistory prop）。
- ✅ generation-quota-card:110 已为 "Pro Plan: 300 generations per month"。
- 🟡 唯一剩余可改（P2，非阻断）：`generation-quota-card.tsx:98/100/112` 对**注册免费用户**写 "per day / today / Daily limit"，但注册免费是 2 credits 一次性（非每日）→ 误导"会重置"。匿名 2/IP/24h 则"per day"成立。建议按 quota.type 区分文案。
- ⚪ 范围外（软隐藏 pokemon 路径）：`pokemon-quota-card.tsx:109` 仍 "Unlimited generations per day" + 链 /pricing；`pricing/client-page.tsx:193` 仅代码注释残留 "Pro Unlimited"（非可见文案）。
