# 2026-07-08

## 全站赠分统一为 2（已完成 + 验证）
用户将新用户赠分从 1 统一为 2，消除"登录反而比匿名吃亏"的倒挂。四处已改并验证：
- app/api/webhooks/new-user/route.ts:31 → `credits: 2`
- app/api/generate-fusion/route.ts:128 → `credits: 2`
- app/api/fusion/generate/route.ts:131 → `credits: 2`
- app/api/credits/route.ts:49,55 → `credits: 2` / `initial_credits: 2`

grep 确认 app/ 下无残留 `credits: 1`。现状对齐：匿名 2 次/IP = 注册 2 credits = 文案 "2 free fusions to start"。倒挂消除。

### 残余小不对称（非阻塞，可选后续）
- /ai 登录免费用户是**终身 2 credits**（用完即 0 需升级）；dragon-ball/pokemon 登录免费是 **3 次/天** 刷新。两者都 ≥ 匿名 2，无倒挂，但 /ai 免费无每日刷新，是体验不对称，非 bug。
- 未动项仍待处理：dragon-ball 死点击(P1)、配额耗尽弹窗强化付费引导(P1)、config/subscriptions.ts 死配置(P2)。

## P1 收尾三项（用户完成 + 本回合验证）
1. **死点击**：components/dragon-ball/fusion-studio.tsx 的 CharacterButton 增加 `disabled` prop，生成中 `disabled={isGenerating}`（line 1006/128/133），按钮加 disabled 样式（opacity/cursor-not-allowed）。验证通过 ✅
2. **配额弹窗（仅 dragon-ball）**：
   - 匿名(`!user`)：Sign In / Sign Up + ghost 链接 "Skip - Upgrade to Pro directly 🚀" → /pricing?source=dragon_ball_quota_anon (line 1325-1329) ✅
   - 已登录(`user && !hasQuotaAccessValue`)："Upgrade to Pro - Unlimited Fusions 🚀" + 价值点 "300 fusions/month · No watermark · HD download · Commercial license" (line 1343-1353) ✅
3. **死配置**：删除 config/subscriptions.ts。验证 config/ 目录已空，且无任何 import 引用（残留 "subscriptions" 匹配均为 @/utils/supabase/subscriptions、@/types/subscriptions、Supabase 表名，非被删文件）。删除安全 ✅

### 一致性缺口（非阻塞，建议后续）
配额弹窗改动**只应用到 dragon-ball**，pokemon 与 /ai 仍是旧逻辑：
- pokemon/fusion-studio.tsx:662-677 已登录弹窗文案为 "Upgrade to VIP"（与全站 "Pro" 命名不符），且无价值点、无匿名 "Skip" 链接。
- app/ai/client-page.tsx:960-992 匿名 guest_limit 弹窗仅 Sign In / Create Free Account，无 "Skip - Upgrade to Pro directly"。
因 funnel 数据显示 pokemon/ai 流量≈0，影响有限；但命名("VIP" vs "Pro")与付费引导不一致，建议择机对齐三工作室。

## 配额弹窗三工作室统一（用户完成 + 本回合验证）
用户将 dragon-ball/pokemon/ai 的配额弹窗统一为：匿名 Sign In/Sign Up Free/Skip→Pro；已登录 "Upgrade to Pro - Unlimited Fusions 🚀" + 价值点(300/月·去水印·HD·商用)。验证三处一致：
- dragon-ball:1327/1348/1352、pokemon:651/671/675、ai:1004/1028/1031。✅

### 但 "VIP" 命名仍残留（本回合发现，用户称"命名统一为 Pro"未完全达成）
- 工作室内 toast/状态文案仍写 "Upgrade to VIP"：dragon-ball:299(状态)、:635(toast)、:711(toast)；pokemon:272(状态)、:435(toast)。→ 同一交互先弹 "VIP" toast 再弹 "Pro" 弹窗，自相矛盾。
- dashboard 配额卡整卡仍 "VIP" 主题且数字冲突：components/dashboard/generation-quota-card.tsx:98/121（"Upgrade to VIP for more!" / "Upgrade to VIP for 10 generations/day"）、pokemon-quota-card.tsx:98/111。这些卡显示 "10 generations/day"，与定价/弹窗的 "Pro = 300/month" 直接矛盾。
建议：把上面 toast/状态/dashboard 卡的 "VIP" 全部改为 "Pro"，并把 dashboard "10 generations/day" 与真实 Pro 额度对齐（或改为指向 /pricing 不再硬编码数字）。

## 后端 Pro 额度对齐（架构项，待用户拍板）
用户已把 generate-fusion/route.ts:104 错误串改为 'Daily limit reached for Pro plan.' ✅
读 lib/rate-limit.ts 后发现的真实状态：
- checkVIPUserDailyQuota(109-139) / getVIPQuotaReadOnly(209-228) 都按**每日** key `quota:vip:${userId}:${today}`、limit=10、过期86400s → dragon-ball/pokemon 的 Pro 被 **10/天** 卡住。
- 但 /ai（fusion/generate/route.ts，P0 已让 VIP 跳过 credits 门禁）Pro 实际是**无限**。→ 三工作室对 Pro 处理不一致（dragon-ball/pokemon 比 /ai 少 290 次/月），是真实功能不对称 bug。
- 文案自相矛盾：gate 按钮写 "Upgrade to Pro - **Unlimited Fusions**"，定价页写 "Pro = **300/month**"。改后端前需先定 Pro 是无限还是 300/月。
方案 A（推荐）：去掉日限制，Pro=无限 = 与 /ai + gate 文案一致、删 Redis 计数面、最省；代价：定价页 "300/month" 改 "Unlimited"。
方案 B：改按月计数 300/月 = 与定价页一致；代价：gate "Unlimited" 改 "300/month"，且 key 改 `${YYYY-MM}`、过期~30天。
**用户选方案 B：Pro = 300/月（按月计数）**。

### 实施（本回合已完成代码改动）
- lib/rate-limit.ts：`checkVIPUserDailyQuota`→`checkProUserMonthlyQuota`，`getVIPQuotaReadOnly`→`getProQuotaReadOnly`；key 改为 `quota:pro:${userId}:${YYYY-MM}`，limit=300，过期=到月底秒数（自然月滚动）。
- app/api/generate-fusion/route.ts：import/call 改名；错误串→'Monthly limit reached for Pro plan (300 fusions/month).'；返回 `limit:300`、`type:'monthly_limit'`。
- app/api/get-quota/route.ts：import/call 改名；返回 `type:'monthly_limit'`。
- app/ai/client-page.tsx：`type` 联合加 `'monthly_limit'`；gate 按钮 "Unlimited Fusions"→"300 Fusions/month"。
- components/dragon-ball & pokemon fusion-studio.tsx：gate 按钮 "Unlimited Fusions"→"300 Fusions/month"。
- components/dashboard/generation-quota-card.tsx & pokemon-quota-card.tsx：Pro 用户状态改 "Monthly limit reached. Resets next month." / "Last generation remaining this month"（原 "Daily...Resets tomorrow" 对按月 Pro 错误）。
grep 确认无残留 checkVIPUserDailyQuota/getVIPQuotaReadOnly/daily_limit/Unlimited Fusions/Resets tomorrow。tsc --noEmit 后台验证中。

### 残余（已知、未改）
- **/ai 工作室 Pro 仍为无限**（fusion/generate/route.ts P0 跳过 credits 门禁，未接月度配额）。改后 dragon-ball/pokemon=300/月，/ai=无限 → 三工作室 Pro 额度仍不对称（/ai 多给）。功能无害（超额交付），但若要严格 300/月统一，需给 /ai 也加月度配额检查。已向用户提出，待其决定。
- 定价页 "300/month" 文案现在与后端、gate 按钮、dashboard 全部一致 ✅。

## 软隐藏 /pokemon、/ai，聚焦 /dragon-ball（已完成）
用户决策：前台不显示 /pokemon、/ai 入口，但路由与内容保留（软隐藏，非重定向/删除）。
- 用户本人已完成大部分入口移除（header mainNavItems、footer、home cta/how-it-works、pricing CTA、auth 页、dashboard 快捷入口、contact/about/terms、gallery 内页、blog、site-map 页 studio 条目）。
- 本回合补完用户遗漏的 3 处并清理：
  1. components/home/features-section.tsx:76 Pokemon 功能卡 `href="/pokemon"` → `/dragon-ball`（保持布局，与用户已改的 AI 卡一致）
  2. components/dragon-ball/cta.tsx:27 删除 "You might also like → Try Pokemon Fusion Generator" 交叉推广块；并移除因此闲置的 `Zap` import
  3. app/gallery/page.tsx:51 "Try the Generator" `href="/ai"` → `/dragon-ball`
- 全仓 grep 复核：UI 层已无任何指向 /pokemon、/ai 工作室的可见链接。剩余引用均为保留项——app/pokemon、app/ai 路由本身；XML sitemap.ts（仅爬虫）；app/api/seo-health 内部检查；proxy.ts 的 /ai 代理中间件；图片资源；博客文章 URL（非工作室链接）。
- tsc --noEmit 通过（EXIT 0）。软隐藏达标，URL 直接访问 /pokemon、/ai 仍可正常打开。

## 漏斗全环节二次审查（2026-07-08 下午，纯审查未改码）
对"访问→使用→注册→付费"四环节重新走查（代码已含前述 P0/P1/软隐藏/额度对齐）。亲自验证后的**真实残留问题**：
- 【P1 真实泄漏】pricing/client-page.tsx:33 未登录点 "Get Pro Unlimited" 跳 `/sign-in?next=/pricing`，但 sign-in 只读 `redirect_to`（sign-in/page.tsx:25）、signInAction 只认 redirect_to（actions.ts:90-92）→ 登录后落到 /dashboard，匿名→Pro 结账意图丢失。应改 `redirect_to=` + encodeURIComponent('/pricing')。
- 【P1 虚假承诺】定价页 "Unlimited Relax Mode"（client-page.tsx:221）且 FAQ:366 "exceed it, still unlimited in Relax Mode"，但 generate-fusion/route.ts:100-108 Pro 到 300/月直接 429 硬挡、无 relax 分支。前后矛盾，伤体验+合规风险。
- 【P1 SEO/聚焦冲突】app/sitemap.ts:27-28 仍收录 /pokemon、/ai（priority 0.8）；app/layout.tsx:29,33,42 + app/page.tsx 全局 title/description 仍写 "Dragon Ball & Pokemon"，与聚焦决策矛盾。
- 【P2 数据隐患】generate-fusion/route.ts:112 Pro 分支 `limitQuota = 10`（应为 300，与 get-quota/route.ts:64 的 300 不一致）；当前 studio 未渲染 limit，未来加进度条会显示 x/10。
- 【agent 误报已纠正】R1（signUpAction 成功分支丢 redirect_to，actions.ts:57-61）：实际 email 验证链接仍携带 redirect_to（emailRedirectTo 已嵌入），仅"手动登录"兜底丢失上下文，属 P2 微调而非 P0 断点。原"注册后跳 /dashboard 弹回"断点确已修好。
- 其余 agent 报的 P2（死组件 generation-quota-card/pokemon-quota-card 含 limit:0 除零隐患、死 API 路由 api/fusion/generate、console.log PII、new-user webhook 发管理员而非欢迎邮件、dashboard credits 与 Pro 额度双口径、seo-health 含 /pokemon /ai、大量 metadata "Dragon Ball and Pokemon"）均待清理，非漏斗硬阻断。

**验证 OK（无问题项）**：三档额度门禁一致（anon2/free2/Pro300）、配额弹窗 Skip→Pro 与 Upgrade→Pro 跳转正确、赠分口径一致(2)、Google 主按钮、Pro 不被 402、软隐藏无前台死链、pricing "Start Creating Free"→/dragon-ball。

### 长期产品方向（已记入项目 MEMORY.md）
当前战略聚焦单一工作室 /dragon-ball；/pokemon、/ai 为软隐藏状态（路由保留、前台无入口）。后续若要做功能/付费改动，默认以 dragon-ball 为准，其余两个工作室暂不作为重点。

## 本轮 9 处修复复核（用户自改，我纯核对，未改码）
全部确认改对：
1. pricing/client-page.tsx:33 `?next=` → `?redirect_to=${encodeURIComponent('/pricing')}` ✅（堵匿名→Pro 结账意图泄漏）
2. pricing:221 "Unlimited Relax Mode" → "300 Generations / Month" ✅
3. pricing:366 FAQ 答案 "Unlimited Relax" → "300 fast generations per month" ✅（与后端 300 硬封顶一致）
4. layout.tsx:29,33,42 全局 title 去 "& Pokemon" → "Dragon Ball AI Fusions" ✅
5. page.tsx:14,21,38,51,62 首页 metadata + 结构化数据去 Pokemon ✅
6. sitemap.ts:27-28 移除 /pokemon、/ai 收录 ✅
7. generate-fusion/route.ts:112 VIP `limitQuota=10` → 300 ✅（与 get-quota 的 300 对齐）
8. credits/route.ts:82 `amount:1` → 2 ✅（与 new-user 赠分 2 一致）
9. pricing:252 `nr-2` → `mr-2` ✅（nr-2 非合法 Tailwind 类、无效样式；mr-2 给 Loading 图标加正确右间距）

小提醒（非错误）：pricing:365 FAQ 问题标题仍写 `Is the "Unlimited" really unlimited?`，答案已改 300/月，标题可顺带改口径；credits:82 的 amount=2 是历史流水、与 new-user 余额 upsert 均为 2，无 1/2 不一致。

至此四环节漏斗（访问→使用→注册→付费）的 P0/P1/P2 转化断点、额度矛盾、聚焦/SEO 口径冲突均已闭环。仅余非阻断的清理项（死组件/死路由/console.log PII/webhook 发管理员/大量 metadata "and Pokemon" 残留）。

## 重新全面审查（2026-07-08 晚，用户要求"重新来次"）
由 Explore agent 做 very thorough 扫描 + 我亲自读代码验真。重要更正：**上轮 #4/#5 的 title 去 Pokemon 改动未持久化（被回退）**，当前磁盘状态 Pokemon 仍在多处。

### 【P0 真问题·已亲验】
- **app/api/webhooks/creem/route.ts:107-156**：`subscription.active` 时 `credits: currentCredits + grantAmount`（300/月、3600/年），**无幂等保护**。Creem webhook 至少一次投递 + 每期续费都重发 → 重复累加白送额度、customers.credits 无限膨胀（Pro 实际走 Redis 月度配额、不消耗该字段，导致 dashboard 显示永不减少的虚高数字）。修复：用 creem_subscription_id 做幂等（加 welcome_credited 标志或仅在状态由非 active→active 时发一次）。
- **PII 日志**：creem/route.ts:49 `console.log` 打印含邮箱的完整 payload；generate-fusion/route.ts:209-212 打印 user.email/clientIP/fullPrompt；new-user/route.ts:38 打印 email。生产环境应删除/脱敏（GDPR/CCPA 风险）。

### 【P1 真问题·已亲验】
- **title/metadata 去 Pokemon 被回退**：layout.tsx:29,33,42、page.tsx:14,21,38,51,62、footer.tsx:40、terms/privacy/site-map/gallery 仍写 "Dragon Ball & Pokemon"。需重新改（上次验证时是对的状态，现已回退）。
- **定价 "Save 17%" 算错**：pricing/client-page.tsx:95。月$9.99×12=$119.88，年$79.99 → 实际省 33%，非 17%。低估年付吸引力。改 "Save 33%"（并人工核对 Creem 后台三档价=$9.99/$79.99/$4.99）。
- **dragon-ball 残留 "Unlimited fusions" 文案（7 处）**：fusion-studio.tsx:279/299/635/711/1299/1550/1741。Pro 实际 300/月硬上限，写 Unlimited 误导已付费用户。需统一改 "300 fusions/month"。
- **dashboard CreditsBalanceCard 对 Pro 显示用不到的 credits**：credits-balance-card.tsx 只显示 customers.credits 裸数字（Pro=300/3600），但 Pro 不消耗该字段 → 虚高且概念混乱。Pro 应改显 "Pro · 300/month（已用 X）"。
- **注册页仍强制邮箱验证+弹回登录**：actions.ts:57-61 注册成功跳 /sign-in 提示查邮件。已知产品取舍（Google 为主按钮），但若想提转化可关邮箱确认或验证后自动登录。

### 【P2 真问题】
- Pro 满额(429)仍提示 "Upgrade to Pro"（已是 Pro，无解）→ 应提示"本月已用 300，下月重置"。
- creem/route.ts:167 refill product id 硬编码兜底 `prod_2u5vQK9gqpiGF8mjKsZksb`（env 缺失即误判）。
- checkout/route.ts 残留 DEBUG console.log。
- dashboard 两张 quota 卡(generation-quota-card/pokemon-quota-card)死代码含 limit:0 除零隐患 + Pokemon 文案 → 删除。
- /api/fusion/generate 仅被隐藏的 /ai 调用。
- pricing:120 Free 卡 "Try all fusion generators" 暗示多工作室 → 改 "Try Dragon Ball Fusion free"。
- pricing:365 FAQ 问题标题仍 "Is the 'Unlimited' really unlimited?"（答案已改 300/月）。
- sign-up/page.tsx:13 metadata 仍提 Pokemon。
- 大量页面 metadata "Dragon Ball and Pokemon"（terms/privacy/about/contact/gallery/blog 等）待批量替换。

### 健康项（已确认无需改）
三档额度门禁一致(2/2/300)、Pro 不被 402、配额弹窗 Skip→Pro/Upgrade→Pro 跳转正确、软隐藏达标(无前台 pokemon/ai 链接)、登录 redirect_to 闭环(定价登录后回 /pricing)、Google 主按钮、赠分口径一致(2)。

## 交叉核验第三方审计 16 项（用户贴出另一 AI 的报告，我逐条读代码亲验）
结论：**16 项事实基本全部成立**，仅 2 处需校正。已合并进 `funnel-audit-2026-07-08.html`（新增"交叉核验"专节）。

- **#1 Pro 角色泄漏（原 Critical）→ 确认真实，且更严重**：broly/jiren/whis/vegito/gogeta/gotenks 均 `pro:true`（dragon-ball-data.ts:61,90,133,141,149,157）；`getRandomCharacters:244` 不过滤；`randomize:549 / swapLeftFighter:562 / swapRightFighter:576` 直接 setChar 绕过 `selectCharacter:489` 的锁；`generate-fusion/route.ts:152` API 也不二次校验 → 免费用户经 Random/Swap 拿到 Pro 角色后**真的能生成融合图**（仅带水印）。变现泄漏。修：getRandomCharacters 对免费用户过滤 pro + generateFusion 生成前加 pro 校验。
- **#2 Refill 重复加积分（原 Critical）→ 确认真实**：creem/route.ts:162-201 中 checkout.captured+completed+invoice.paid 三事件各 +100 无幂等。与我前次发现的 subscription 路径(+300/3600)无幂等同源，合为 P0 漏钱。
- **#3 注册丢 redirect_to（原 Critical）→ 降 P2**：actions.ts:57-61 成功分支没传 getAuthRedirectParams(redirect_to)，但验证邮件 emailRedirectTo 已内嵌 redirect_to，仅"手动登录"兜底丢上下文，影响小。
- **#4/#5/#6 Unlimited 文案**（cta:31 / pricing:192 "Pro Unlimited" / subscription-status-card:160 "unlimited fusions"）→ 均确认真实，与 300/月矛盾。
- **#7 匿名生成失败也扣 quota**（generate-fusion:62 incr 在生成前、Fal 失败不回滚）→ 真实，免费用户白丢 1 次。
- **#8 "No thanks" 不隐藏 banner**（fusion-studio:1414 仅 trackEvent）→ 真实。
- **#9 配额 banner 仅双空位显示**（fusion-studio:308）→ 真实（Minor）。
- **#10 dashboard credits_history 硬编码空**（dashboard/page.tsx:61 `recentCreditsHistory=[]`）→ 真实，永远 "No recent activity"。
- **#11 注释"1 Credit"实际 2**（generate-fusion:125）→ 真实（极小，仅注释错）。
- **#12 generation-quota-card 死代码+NaN**（limit:0 除零）→ 确认死代码（全仓无 import 渲染），降 P3 清理。
- **#13 扣款失败静默放行**（generate-fusion:257-266 updateError 仅 console.error 未 throw）→ 真实，DB 出错时白嫖生成。
- **#14 checkout console.log env key 名**（checkout/route.ts:41 Object.keys(process.env)）→ 真实（小，信息泄漏）。
- **#15 refill product id 硬编码兜底**（creem/route.ts:167）→ 真实（小，有 env 兜底）。
- **#16 resetPasswordAction 缺 return**（actions.ts:138,146,158,165）→ 真实 bug，校验不退出、函数可能不重定向。

**合并后 P0 漏钱项 = Creem 两路无幂等**：subscription.active (+300/3600) + refill 三事件(+100×3)。均应加幂等去重。

## 本轮 7 项修复复核（用户自改，我纯核对，未改码）
用户给出"本轮修复总结"表（P0×3 + P1×4）。逐条读代码亲验，全部成立：

### P0（3 项）
1. **Webhook 幂等**：creem/route.ts
   - subscription 路径(106-158)：select `last_credited_period_end`(110)，`if (customer?.last_credited_period_end === currentPeriodEnd) break`(115) → 同账期续费/重发事件去重 ✅ 稳健（period_end 按账期稳定）。
   - refill 路径(164-216)：select `last_refill_event_id`(184)，`if (customer?.last_refill_event_id === eventId) break`(189) → 同 event id 重发去重 ✅。
   - ✅ refill 幂等已改为 **5 分钟冷却窗口**（用户本轮二次修复）：creem/route.ts:180-194 不再依赖 eventId（不同事件类型 id 不同），而是读 `last_refill_event_id`（现语义改为存 ISO 时间戳，:205/:212 写入 `new Date().toISOString()`），`elapsed = Date.now() - new Date(lastGrant)`，`if (elapsed < 5*60*1000) break`。无论 Creem 推 checkout.completed 还是 invoice.paid，同笔购买的两次事件都在 5 分钟内 → 只 +100 一次。原"多事件同笔购买双计"风险已消除。
   - 🔸 新残余边角（非阻断）：① 冷却是**按用户全局时间窗**非按笔：若用户 5 分钟内**合法**连买两包 refill（想拿 200），第二包会被吞掉（少发 +100，但已收款）→ 反向少发。发生概率低，是"防多发"对"防少发"的取舍。② 真正并发的两次投递存在 TOCTOU（都读到旧值→都过检查→+200）；低流量 refill 端点罕见。若要零双向风险，最优是同时按 `checkout_id` 去重（invoice.paid 与 checkout.completed 同笔应带同一 checkout_id）。
2. **PII 日志清理**：generate-fusion/route.ts:213 `// Debug logging removed for PII compliance`（原 209-212 打印 email/IP/prompt 已删）✅；checkout/route.ts:39 仅打印 env **变量名**（非 secret 值，可接受）；new-user/route.ts 无 email 打印 ✅；creem/route.ts 无 email/prompt/IP 打印（仅 userId UUID，必要可留）。
3. **硬编码 product id 兜底 → 缺失即报警**：creem/route.ts:167 `const refillId = process.env.CREEM_PRODUCT_ID_REFILL;` + 168-172 `if (!refillId) { console.error("CREEM_PRODUCT_ID_REFILL not configured"); break; }` ✅（去掉了原 `prod_2u5vQK9gqpiGF8mjKsZksb` 硬编码兜底）。

### P1（4 项）
4. **7 处 "unlimited" → "300 fusions/month"**：dragon-ball/fusion-studio.tsx grep `unlimited` = No matches；"300 fusions/month" 出现 6 处(279/299/635/711/1299/1352) ✅（原审计列的 7 处含 1 处已并入同义表述，结论：dragon-ball 文案已无 Unlimited）。
5. **"Save 17%" → "Save 33%"**：pricing/client-page.tsx:95 `(Save 33%)`、:179 `SAVE 33%` ✅（与 $9.99×12=$119.88 vs $79.99 = 33% 一致）。
6. **Pro 角色泄漏闭环**（原 #1 Critical）：
   - 前端 4 路全过滤免费用户 pro：selectCharacter:489 `if (char.pro && !quota.isVIP)`、swapLeftFighter:563 `(!fighter.pro || quota.isVIP)`、swapRightFighter:577 同、randomize:550 `getRandomCharacters(2, !quota.isVIP)`、UI 锁:1005 `isProLocked={Boolean(character.pro) && !quota.isVIP}` ✅
   - API 二次校验（兜底）：generate-fusion/route.ts:159-162 `if ((char1.pro || char2.pro) && !isVIP) return 403 PRO_REQUIRED` ✅ 免费用户经任何前端漏洞拿到 Pro 角色，生成时仍被 403 挡下，不再白送真图。
7. **"Unlimited generations" → "300 fast fusions/month"**：subscription-status-card.tsx:160 `Get 300 fast fusions per month...` ✅；dragon-ball/cta.tsx 当前无 "Unlimited generations" 残留（已清）✅。

### 残余小项（非阻断，本次未列入范围）
- fusion-studio.tsx:80 Pro 剩余额度显示 `"∞"`（与 300/月硬上限口径略矛盾，仅显示、后端仍封顶）。建议 Pro 改显剩余数字或"300/month"。
- dashboard generation-quota-card.tsx:110 与 pokemon-quota-card.tsx:109 仍写 "Pro Plan: Unlimited generations per day"（前者疑死代码、后者属软隐藏 pokemon 路径）。
- 非 dragon-ball 页 metadata/文案仍含 "unlimited"（pricing/page.tsx 3×、sign-in/page.tsx:13、about:166、contact:130、ai/*）——与全站 300/月定位不符，建议择机批量对齐（原 P2 清理项）。

**结论**：本轮 P0×3 + P1×4 全部真实落地、验真通过。原 P0#1 refill "多事件同笔购买双计"风险已由 5 分钟冷却窗口消除（见上文 ✅）。其余漏斗断点、额度矛盾、聚焦/命名、Pro 泄漏均已闭环。仅余非阻断清理项（见上文 🔸/残余小项）。

---

## 🔴 漏斗复检（晚间轮，2026-07-08）：发现 Pro 付费墙已**回归失效**

应"从访问→使用→注册→付费全链路复检"要求，逐文件重读源码，发现上一轮已验证通过的 **Pro 角色泄漏修复已被整体回退**，且比最初更严重（后端 403 兜底也没了）：

**证据（当前源码）**
- `app/api/generate-fusion/route.ts`：全文无 `PRO_REQUIRED` / `char1.pro` / `char2.pro` → 后端已无 pro 判断，查到角色直接进 Fal 生成。
- `components/dragon-ball/fusion-studio.tsx:1066`：`characterGrid` 传 `isProLocked={false}`（硬编码）→ Pro 角色无视觉锁定/角标。
- `selectCharacter`（:548-596）无 `character.pro` 判断；`randomize`（:599）调 `getRandomCharacters(2, false)`（应为 `!quota.isVIP`）；`swapLeftFighter`（:611）/ `swapRightFighter`（:625）filter 未排除 pro。
- 前端无 `PRO_REQUIRED` 错误分支。
- `lib/dragon-ball-data.ts` 仍保留 `getRandomCharacters(count, excludePro)` 支持（:244-245）+ 6 个 `pro:true` 角色。

**后果**：免费/匿名用户当前可真实生成 Broly/Jiren/Whis/Vegito/Gogeta/Gotenks。**直接漏钱 + 贬低 Pro 层**，P0 致命。

**建议修复（前后端同步）**
1. 后端兜底（最关键）：generate-fusion/route.ts 角色查到后、进 Fal 前加 `if ((char1.pro||char2.pro) && !isVIP) return 403 {code:'PRO_REQUIRED', upgradeUrl:'/pricing'}`。
2. 前端：`randomize`→`getRandomCharacters(2, !quota.isVIP)`；swap 两函数 filter 加 `&& (!f.pro || quota.isVIP)`；`characterGrid`→`isProLocked={!!character.pro && !quota.isVIP}`；`selectCharacter` 入口加 pro 拦截 + toast；generateFusion catch 识别 `PRO_REQUIRED` 弹升级层。

**其余发现（非阻断）**
- 定价页命名/话术矛盾：client-page.tsx:217 "Pro Unlimited"、:281 "Get Pro Unlimited"、:375 脚注 "Unlimited usage…" 与全站 "300 fusions/month" 定位冲突；:145 Free 卡 "Try all fusion generators" 暗示已隐藏的 pokemon/ai。
- 认证页 metadata 仍含 "unlimited … Pokemon"（sign-in/page.tsx:13、sign-up/page.tsx:13）。

**已确认健康、勿动**：webhook 3 层幂等、PII 清理、匿名 2 次+爆刷保护+额度弹窗 redirect 闭环、Pro 月度 300 对齐、软隐藏。

报告产物：`.workbuddy/memory/funnel-reaudit-2026-07-08.html`。
